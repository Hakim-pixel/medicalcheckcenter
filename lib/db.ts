// Prisma removed: this file kept as a stub so any leftover imports won't crash the frontend build.
// All data access in the frontend should go through the Laravel API via `fetchFromLaravel`.

const dbStub: any = {};
export default dbStub;
